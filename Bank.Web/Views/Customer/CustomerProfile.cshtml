@model CustomerProfileViewModel

@{
    ViewData["Title"] = "Profile";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div id="customer-profile">
    <div class="container">
        <div class="profile">
            <h1>@Model.FirstName @Model.LastName</h1>

            <div class="profile-group">
                <strong>Contact</strong>
                <p>@Model.Email</p>
                <p>+@Model.PhoneCountryCode @Model.Phone</p>
            </div>

            <div class="profile-group">
                <strong>Gender</strong>
                <p>@Model.DisplayGender</p>
            </div>

            <div class="profile-group">
                <strong>Birthday</strong>
                <p>@Model.DisplayBirthday</p>
            </div>

            <div class="profile-group">
                <strong>Location</strong>
                <p>@Model.Address</p>
                <p>@Model.PostalCode, @Model.City</p>
                <p>@Model.Country, @Model.CountryCode</p>
            </div>
        </div>
        <div class="accounts">
            <h1>Accounts</h1>
            <table id="account-table">
                <thead>
                    <tr>
                        <th>Number</th>
                        <th>Balance</th>
                        <th>Created</th>
                        <th>Manage</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var account in Model.Accounts)
                    {
                        <tr>
                            <td>
                                <a asp-controller="Account"
                                   asp-action="AccountDetails"
                                   asp-route-id="@account.AccountId"
                                   class="account-id">@account.AccountId
                                </a>
                            </td>
                            <td>@account.DisplayBalance</td>
                            <td>@account.DisplayCreated</td>
                            <td>
                                <a asp-controller="Account"
                                   asp-action="AccountDetails"
                                   asp-route-id="@account.AccountId"
                                   class="account-details">Manage
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            <p>Total balance: <span class="total-balance">@Model.DisplayTotalBalance</span></p>
        </div>
    </div>
</div>

